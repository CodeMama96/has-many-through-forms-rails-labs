<%= render 'form', post: Post.new %>

<%= form_for @post do |p|%>
    <%= p.label(:title) %>
    <%= p.text_field :title %>
    <%= p.label(:content) %>
    <%= p.text_field :content %>
    <%= p.submit %>
<% end %>